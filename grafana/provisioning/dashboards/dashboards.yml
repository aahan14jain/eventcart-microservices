apiVersion: 1
providers:
  - name: EventCart
    orgId: 1
    folder: EventCart
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
