{
  "name": "eventcart-realtime",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal Kafka → WebSocket relay for EventCart saga and DLQ events",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2",
    "kafkajs": "^2.2.4",
    "ws": "^8.18.0"
  }
}
