apiVersion: v1
kind: ConfigMap
metadata:
  name: notification-service-config
  namespace: eventcart
  labels:
    app.kubernetes.io/name: notification-service
    app.kubernetes.io/component: consumer
    app.kubernetes.io/part-of: eventcart
data:
  SERVER_PORT: "8083"
  SPRING_PROFILES_ACTIVE: kubernetes
  SPRING_KAFKA_BOOTSTRAP_SERVERS: eventcart-kafka:9092
  MANAGEMENT_ENDPOINT_HEALTH_PROBES_ENABLED: "true"
  MANAGEMENT_ENDPOINT_HEALTH_SHOW_DETAILS: when_authorized
  MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE: health,info,metrics,prometheus
---
apiVersion: v1
kind: Secret
metadata:
  name: notification-service-secret
  namespace: eventcart
  labels:
    app.kubernetes.io/name: notification-service
    app.kubernetes.io/component: consumer
    app.kubernetes.io/part-of: eventcart
type: Opaque
stringData:
  PLACEHOLDER: unused
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: eventcart
  labels:
    app.kubernetes.io/name: notification-service
    app.kubernetes.io/component: consumer
    app.kubernetes.io/part-of: eventcart
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8083
      targetPort: http
  selector:
    app.kubernetes.io/name: notification-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: eventcart
  labels:
    app.kubernetes.io/name: notification-service
    app.kubernetes.io/component: consumer
    app.kubernetes.io/part-of: eventcart
    app.kubernetes.io/version: "0.0.1"
spec:
  replicas: 2
  revisionHistoryLimit: 5
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: notification-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: notification-service
        app.kubernetes.io/component: consumer
        app.kubernetes.io/part-of: eventcart
        app.kubernetes.io/version: "0.0.1"
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8083"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
      terminationGracePeriodSeconds: 60
      containers:
        - name: notification-service
          image: eventcart/notification-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8083
          envFrom:
            - configMapRef:
                name: notification-service-config
            - secretRef:
                name: notification-service-secret
          resources:
            requests:
              cpu: 200m
              memory: 384Mi
            limits:
              cpu: 750m
              memory: 768Mi
          startupProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 30
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          lifecycle:
            preStop:
              exec:
                command: ["sh", "-c", "sleep 10"]
